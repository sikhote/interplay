import { StyleSheet } from 'react-native';
import { spacing } from '../../lib/styling';

export default StyleSheet.create({
  root: {
    maxWidth: 400,
  },
  marginTop: {
    marginTop: spacing.c,
  },
});
