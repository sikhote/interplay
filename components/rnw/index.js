// These are components from react-native-web that we use.
// These components are imported and exported here, incase we need to
// customize them. For example, the Button component now has a loading prop.

import {
  AppRegistry,
  ActivityIndicator,
  View,
  ScrollView,
  Dimensions,
  Image,
  ImageBackground,
  TouchableOpacity,
  TouchableWithoutFeedback,
} from 'react-native-web';
import Button from './Button';
import Text from './Text';
import Switch from './Switch';
import TextInput from './TextInput';

export {
  TextInput,
  Button,
  AppRegistry,
  ActivityIndicator,
  Text,
  View,
  ScrollView,
  Dimensions,
  Image,
  ImageBackground,
  TouchableOpacity,
  TouchableWithoutFeedback,
  Switch,
};
